name: Deploy

on:
  push:
    branches:
      - feat/revamp  # Atau branch lain yang lo mau

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to server
        run: |
          ssh -o StrictHostKeyChecking=no tops1919@46.17.172.35 "cd /home/tops1919/public_html/admin-panel && git pull origin feat/revamp 2>&1"
